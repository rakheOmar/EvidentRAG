# Domain Docs

Single-context repo. Skills should read `CONTEXT.md` at the repo root and `docs/adr/` for architectural decisions before exploring.

If these files don't exist, proceed silently — don't flag their absence.
